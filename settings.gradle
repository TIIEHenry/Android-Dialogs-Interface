rootProject.name = "Android-Dialogs-Interface"
include ':public_api'
include ':wrapped_md_dialogs'
//include ':app'

rootProject.children.each {
    /**设置构建脚本文件名*/
    it.buildFileName ="dialogs-"+it.name + '.gradle'
}